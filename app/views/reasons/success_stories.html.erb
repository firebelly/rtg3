<%= content_for :title, raw(@page.title_override.blank? ? @page.title : @page.title_override) %>

<% unless @promoted_success.blank? %>
  <%= content_for :featured do %>
    <div class="wrap slider-wrap">
      <div class="feature-slider">
        <%= render partial: "reasons/success_story", locals: { success_story: @promoted_success[0] } -%>
      </div>
      <a data-scroll href="#content" class="scroll-down"><svg class="icon icon-arrow" role="img"><use xlink:href="#icon-arrow"></use></svg></a>
    </div>
  <% end %>
<% end %>

<div class="blocks reason-blocks">
	<% @successes.each do |reason| %>
		<%= render partial: 'reasons/list', locals: { reason: reason, use_success_links: true } %>
	<% end %>
</div>
