<li class="accordion -<%= news.post_type.slug=="news" ? "pink" : "gray" %>">
  <div class="block-content">
    <div class="accordion-trigger">
      <h4><%= news.title %></h4>
      <div class="actions" aria-hidden="true">
        <svg class="icon icon-<%= icon_css(news.icon) %>" role="img"><use xlink:href="#icon-<%= icon_css(news.icon) %>"></use></svg>
        <span class="plus-x -<%= news.post_type.slug=="news" ? "white" : "pink" %>"></span>
      </div>
    </div>
    <div class="accordion-content">
      <div>
        <%= raw video_embeds(news.content) %>
        <p class="more"><%= link_to('View Page', news, class: 'btn -white -hollow') %></p>
      </div>
    </div>
  </div>
</li>